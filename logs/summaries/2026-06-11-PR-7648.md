# Conflict Resolution: PR #7648 vs v0.8.0 base advancement

**Date:** 2026-06-11  
**Build:** [BonsaiPR v0.8.6-alpha260611-53bd428 \[asc\]](https://github.com/falken10vdl/bonsaiPR/releases/tag/v0.8.6-alpha2606111559) (`remotes/falken10vdl/build-0.8.6-alpha2606111559`)  
**Build order:** ascending (lowest PR first)  
**Target PR:** [#7648 `extend-profiles-and-extrusions`](https://github.com/IfcOpenShell/IfcOpenShell/pull/7648)  
**Conflicting commit:** [`6bde619fe6`](https://github.com/IfcOpenShell/IfcOpenShell/commit/6bde619fe6) — "Migrate MEPConnectElements args from object names to IFC GUIDs"  
**Result commit:** [`648c328d37`](https://github.com/IfcOpenShell/IfcOpenShell/commit/648c328d37)

---

## Background

PR #7648 (`extend-profiles-and-extrusions`) adds Ctrl+E support for PROFILE elements and basic extrusions. It consists of three commits:

1. `28913afc12` — "closes #3565 - extend profiles and basic extrusions to 3D cursor with Ctrl+E" (adds ~66 lines to `profile.py`, refactors workspace.py Hotkey handler)
2. `9013dd2c35` — "Add 'E' to ExtendProfile join_type enum"
3. `e9ebba9050` — "Add Extend to Cursor button to active tool panel"

The branch was created from `93c6350e0f` (v0.8.0 as of June 7, 2026).

---

## What Caused the Conflict

Between the branch's base commit (`93c6350e0f`, June 7) and the build's base at the time of the 1559 build (`0c993d3292`, June 11 09:28 CEST), the v0.8.0 branch advanced through commit:

**`6bde619fe6`** — "Migrate MEPConnectElements args from object names to IFC GUIDs" (May 22, Gorgious56)

This commit modified `src/bonsai/bonsai/bim/module/model/profile.py` at line ~1157 (the `DrawPolylineProfile` class), changing `mep_connect_elements` to use `obj1_guid`/`obj2_guid` instead of `obj1_name`/`obj2_name`.

PR #7648 also modifies `profile.py` (at lines ~282–381, in the `ExtendProfile` class). Although these regions are non-overlapping, the git 3-way merge algorithm — with LCA at `93c6350e0f` — detected the combination of changes as conflicting in the build environment (Linux server, git on the remote build host). The conflict could not be reproduced locally on Windows, possibly due to minor differences in git version or merge strategy behavior.

The 1559 build (June 11, ascending order) skipped #7648 for this reason, reporting "Merges cleanly against base (conflict with other PRs)." The June 10, 1835 build had successfully merged #7648 (when v0.8.0 was still at `bfa2d789f1`, before `6bde619fe6` entered the base).

---

## Conflict Files

| File | Nature of Conflict |
|------|--------------------|
| `src/bonsai/bonsai/bim/module/model/profile.py` | Both v0.8.0 advancement (`6bde619fe6` at line ~1157) and PR #7648 (`28913afc12` at lines ~282-381) modified this file. Different regions but same LCA triggered 3-way conflict in build environment. |

No conflict in `workspace.py` — no v0.8.0 changes between `93c6350e0f` and `0c993d3292` touched that file.

---

## Build History

| Build | Date | v0.8.0 base | #7648 status |
|-------|------|-------------|--------------|
| [alpha2606101416](https://github.com/falken10vdl/bonsaiPR/releases/tag/v0.8.6-alpha2606101416) | Jun 10 14:16 | pre-`6bde619fe6` | SKIPPED (conflict) |
| [alpha2606101635](https://github.com/falken10vdl/bonsaiPR/releases/tag/v0.8.6-alpha2606101635) | Jun 10 16:35 | pre-`6bde619fe6` | SKIPPED (conflict) |
| [alpha2606101835](https://github.com/falken10vdl/bonsaiPR/releases/tag/v0.8.6-alpha2606101835) | Jun 10 18:35 | `bfa2d789f1` | **MERGED** (fixed) |
| [alpha2606111559](https://github.com/falken10vdl/bonsaiPR/releases/tag/v0.8.6-alpha2606111559) | Jun 11 15:59 | `0c993d3292` | SKIPPED (conflict) |

The June 10 1835 build included #7648 successfully — likely because v0.8.0 was at `bfa2d789f1` and the branch had been fixed. Between 1835 and the June 11 1559 build, v0.8.0 advanced to `0c993d3292` (which includes `6bde619fe6`), reintroducing the conflict.

---

## Fix Strategy: `git merge 0c993d3292` (ancestry-merge, Option A)

**Why ancestry-merge works here:**

PR #7648's code does NOT need to change. Its additions to `profile.py` (lines 282–381) are entirely independent of `6bde619fe6`'s change (line ~1157). The conflict is purely an ancestry problem: the branch's LCA is `93c6350e0f` while the build base is `0c993d3292`.

By merging `0c993d3292` into the branch:
- The new LCA for future builds becomes `0c993d3292`
- When the build (also starting from `0c993d3292`) tries to merge the branch, git sees no base-side diff — the merge only has to incorporate the 3 original commits
- The effective diff in the build's merge commit remains exactly: `profile.py` (+70 lines), `workspace.py` (+10/-3 lines), two new icon files

**Why not `-s ours`:** The `6bde619fe6` change (MEP GUID migration) is genuinely absent from the branch and should be incorporated. A plain merge is correct; `-s ours` would discard the GUID migration from the branch's perspective.

**git 3-way merge mechanics:**

Before fix:
- LCA: `93c6350e0f` (branch point)
- Left (build): `22ba409b7a` — includes `6bde619fe6` via `0c993d3292`
- Right (branch): `e9ebba9050` — does not include `6bde619fe6`
- `profile.py` differs from LCA on BOTH sides → conflict

After fix (new merge commit `648c328d37`):
- LCA: `0c993d3292` (both build and branch now share this ancestor)
- Left (build): `22ba409b7a` — no diff from LCA in conflict files
- Right (branch): `648c328d37` — has the 3 original commits
- `profile.py` differs from LCA on RIGHT side only → clean auto-merge

---

## Execution

```bash
# On extend-profiles-and-extrusions branch (at e9ebba9050)
git merge --no-ff --no-edit 0c993d3292
# Auto-merging src/bonsai/bonsai/bim/module/model/profile.py
# Merge made by the 'ort' strategy.
# 197 files changed (large v0.8.0 advancement incorporated into branch)
# New tip: 648c328d37

# Verification test
git checkout 22ba409b7a -b verify-fix
git merge --no-ff --no-edit extend-profiles-and-extrusions
# Merge made by the 'ort' strategy.
# 4 files changed (only the original PR's 3 commits' changes)
# dm_extend_to_cursor.png, lm_extend_to_cursor.png, profile.py, workspace.py
# CLEAN — 0 conflicts

# Push
git push origin extend-profiles-and-extrusions:extend-profiles-and-extrusions --force-with-lease
# e9ebba9050..648c328d37
```

---

## Result

- **New branch tip:** [`648c328d37`](https://github.com/IfcOpenShell/IfcOpenShell/commit/648c328d37)
- **Push remote:** `origin` (IfcOpenShell/IfcOpenShell)
- **Outcome:** fixed — branch now merges cleanly against any build starting from `0c993d3292` or later
- **PR content:** unchanged — the 3 original commits are preserved with no code changes
