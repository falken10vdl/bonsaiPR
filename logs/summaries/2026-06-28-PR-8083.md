# Conflict Resolution: PR #8083 vs PR #7798 / #7965

**Date:** 2026-06-28  
**Build:** `BonsaiPR v0.8.6-alpha260628-3e699ed [asc]` (build branch `build-0.8.6-alpha2606281435`, last commit `3e699ed265`)  
**Build order:** ascending (lowest PR number first)  
**Target PR:** [#8083 `parametric_dimensions`](https://github.com/IfcOpenShell/IfcOpenShell/pull/8083)  
**Conflicting PRs:** [#7798 `ManualDrawingReference`](https://github.com/IfcOpenShell/IfcOpenShell/pull/7798), [#7965 `inset_section_endpoints`](https://github.com/IfcOpenShell/IfcOpenShell/pull/7965)  

---

## Which PRs conflict and why

PR #8083 (`parametric_dimensions`) was being skipped with "⚠️ Skipped - Conflict with other PRs. Merges cleanly with base."

Three files conflicted:

### `src/bonsai/bonsai/bim/module/drawing/__init__.py`

Both PR #7965 (which subsumes #7798's addition) and PR #8083 inserted new operators **after the same base line** (`operator.AddAnnotationType`):

- **Build (#7798 + #7965):** inserted `operator.AssignManualDrawingReference, operator.UpdateSectionEndpoints`  
- **PR #8083:** inserted `operator.AddElevationAnnotation`  
- LCA had neither → git 3-way merge conflict

### `src/bonsai/bonsai/bim/module/drawing/workspace.py`

Same region, after `add_layout_hotkey_operator(cls.layout, "Add", "S_A", ...)`:

- **Build (#7798 + old #8083 merged into #7965):** added `is_manual_reference` prop UI for ELEVATION/SECTION, then `_DIMENSION_TYPES = {"DIMENSION", "RADIUS", "DIAMETER", "ANGLE", "PLAN_LEVEL", "SECTION_LEVEL"}`  
- **PR #8083 (current/rebased):** added only `_DIMENSION_TYPES = {"DIMENSION", "RADIUS", "DIAMETER", "ANGLE"}` (PLAN_LEVEL/SECTION_LEVEL removed; now handled via `_ELEVATION_TYPES`)  
- LCA (`93c6350e0f`, PR #8148 merged into v0.8.0) had neither → conflict

### `src/bonsai/bonsai/tool/drawing.py`

In `create_annotation_object`, after the `PLAN_LEVEL` elif block:

- **Build (#7798):** added `elif object_type == "ELEVATION":` and `elif object_type == "SECTION":` cases  
- **PR #8083:** added `elif object_type == "SECTION_LEVEL":` case  
- LCA had neither → conflict at the same elif chain position

---

## LCA and ancestry analysis

The true LCA between the build tip (`ee4ba4d00c`) and current `parametric_dimensions` was `93c6350e0f` — the merge of PR #8148 (`bonsai/parametric-framework-features-pt2`) into `v0.8.0`, which is a shared ancestor of both.

PR #7965's tip `738110bed7` was the "Merge parametric_dimensions (PR #8083) to resolve build conflict" commit — it merged the **old** PR #8083 tip (`913c9c42`) into #7965. PR #7798's tip `3409c656ca` similarly had the old #8083 tip as ancestor.

The old PR #8083 tip `913c9c42` **is already an ancestor** of the current `parametric_dimensions` (absorbed via `23fd50eace "Absorb old-pd ancestry to fix build merge conflict with PR #7798"`). The LCA between PR #7965 and current `parametric_dimensions` was exactly `913c9c42`.

---

## Key commit

The primary conflict-introducing commit is the PR #7965 tip: [738110bed7](https://github.com/IfcOpenShell/IfcOpenShell/commit/738110bed7) — "Merge parametric_dimensions (PR #8083) to resolve build conflict" — which brought in PR #7798's `AssignManualDrawingReference`, `UpdateSectionEndpoints`, `is_manual_reference` UI, ELEVATION/SECTION cases, **plus** the old #8083 `_DIMENSION_TYPES` with PLAN_LEVEL/SECTION_LEVEL. The current rebased #8083 diverges from that old content.

---

## Why the chosen strategy works

**Strategy: Option A — plain `git merge 738110bed7`** (ancestry fix with manual content resolution).

After merging PR #7965's tip into `parametric_dimensions`:
- The LCA between (build tip `ee4ba4d00c`) and (fixed `parametric_dimensions`) shifts from `93c6350e0f` to `738110bed7`.

From the new LCA (`738110bed7`):
- **`__init__.py`:** LCA already has `AssignManualDrawingReference` and `UpdateSectionEndpoints`. Build adds nothing more in that region. Fixed-pd adds `AddElevationAnnotation` → only one side changes → auto-take it, no conflict. ✓
- **`workspace.py`:** LCA has `is_manual_reference` block + `_DIMENSION_TYPES = {..., PLAN_LEVEL, SECTION_LEVEL}`. Build adds nothing more in that region. Fixed-pd changes `_DIMENSION_TYPES` to omit PLAN_LEVEL/SECTION_LEVEL → only one side changes → auto-take it, no conflict. ✓
- **`tool/drawing.py`:** LCA has ELEVATION/SECTION elif blocks. Fixed-pd adds SECTION_LEVEL elif block. Build adds nothing more there → auto-take it, no conflict. ✓

---

## Conflicts resolved

During the merge of `738110bed7` into `parametric_dimensions`, three conflicts were resolved:

**`drawing/__init__.py`:**  
Kept both sides' additions:
```python
operator.AddElevationAnnotation,       # from current #8083
operator.AssignManualDrawingReference, # from #7798
operator.UpdateSectionEndpoints,       # from #7965
```

**`drawing/workspace.py`:**  
Kept `is_manual_reference` block from #7965 AND used current #8083's narrower `_DIMENSION_TYPES`:
```python
if object_type in ("ELEVATION", "SECTION"):
    row = cls.layout.row(align=True)
    row.prop(cls.props, "is_manual_reference")

_DIMENSION_TYPES = {"DIMENSION", "RADIUS", "DIAMETER", "ANGLE"}  # no PLAN/SECTION_LEVEL
```

**`tool/drawing.py`:**  
Kept all three elif branches:
- `elif object_type == "SECTION_LEVEL":` (from current #8083)
- `elif object_type == "ELEVATION":` (from #7798)
- `elif object_type == "SECTION":` (from #7798)

---

## Files changed

| File | Change |
|------|--------|
| `src/bonsai/bonsai/bim/module/drawing/__init__.py` | Added `AssignManualDrawingReference`, `UpdateSectionEndpoints` alongside `AddElevationAnnotation` |
| `src/bonsai/bonsai/bim/module/drawing/workspace.py` | Added `is_manual_reference` UI block for ELEVATION/SECTION, kept `_DIMENSION_TYPES` without elevation types |
| `src/bonsai/bonsai/tool/drawing.py` | Merged SECTION_LEVEL, ELEVATION, and SECTION elif chains; plus all of PR #7965's other drawing.py additions (MANUAL_DRAWING_REFERENCE type, get_annotation_data_type guard, is_manual_drawing_reference, set_manual_drawing_reference, etc.) |

---

## Verification

Tested with `git merge-tree --write-tree`:

1. Against `v0.8.0 + PR#7965` synthetic base → exit 0 ✓  
2. Against actual build tip `ee4ba4d00c` → exit 0 ✓  
3. Against `v0.8.0` alone → exit 0 ✓  

---

## Push target and result

- **Push remote:** `origin` (`IfcOpenShell/IfcOpenShell`)  
- **Branch:** `parametric_dimensions` (PR head branch lives directly on origin)  
- **Result commit:** [456d2ae501](https://github.com/IfcOpenShell/IfcOpenShell/commit/456d2ae501)
