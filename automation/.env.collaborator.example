# BonsaiPR collaborator test configuration
# Copy to .env on your machine and replace all placeholder values.
#
# This template is for independent testing on a collaborator machine.
# If you do not have write access to falken10vdl repos, use your own forks.

# GitHub token with repo write permissions for the target repos
GITHUB_TOKEN=ghp_replace_with_your_token

# Local paths on YOUR machine
BASE_CLONE_DIR=/home/youruser/bonsaiPRDevel/IfcOpenShell
BUILD_BASE_DIR=/home/youruser/bonsaiPRDevel/bonsaiPR-build
REPORT_PATH=/home/youruser/bonsaiPRDevel/reports
WORKING_DIR=/home/youruser/bonsaiPRDevel/MergingPR

# Optional filtering
USERNAMES=""          # empty = all PR authors
EXCLUDED=""          # comma-separated PR numbers to exclude

# Optional repo overrides (for collaborator forks)
# GITHUB_OWNER=your-github-user
# GITHUB_REPO=bonsaiPR
